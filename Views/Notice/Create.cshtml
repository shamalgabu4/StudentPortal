@model StudentPortal.Models.Notice
@{
    ViewData["Title"] = "Create Notice";
}

<div class="mb-4">
    <h3 class="mb-0">Create New Notice</h3>
    <small class="text-muted">Add an announcement for students</small>
</div>

<div class="row">
    <div class="col-lg-8">
        <form asp-action="Create" method="post">
            <div class="card">
                <div class="card-body">
                    <div class="mb-3 form-floating">
                        <input type="text" asp-for="Title" class="form-control" placeholder="Title" required />
                        <label asp-for="Title">Title</label>
                        <span asp-validation-for="Title" class="text-danger small"></span>
                    </div>

                    <div class="mb-3 form-floating">
                        <select asp-for="Category" class="form-select" required>
                            <option value="General">General</option>
                            <option value="Exam">Exam</option>
                            <option value="Sports">Sports</option>
                            <option value="Events">Events</option>
                        </select>
                        <label asp-for="Category">Category</label>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Content" class="form-label">Content</label>
                        <textarea asp-for="Content" class="form-control" rows="6" placeholder="Enter notice content" required></textarea>
                        <span asp-validation-for="Content" class="text-danger small"></span>
                    </div>

                    <div class="mb-3 form-check form-switch">
                        <input type="checkbox" asp-for="IsActive" class="form-check-input" checked />
                        <label asp-for="IsActive" class="form-check-label">Active (visible to students)</label>
                    </div>
                </div>
            </div>

            <div class="mt-3">
                <button type="submit" class="btn btn-primary"><i class="bi bi-check"></i> Create Notice</button>
                <a class="btn btn-secondary" asp-action="Manage">Back</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
